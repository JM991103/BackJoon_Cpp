#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	/*long long n, m;
	cin >> n >> m;
	cout << (n + m) * (n - m) << "\n";*/

	/*int n;
	double a;
	cin >> n;

	for (int i = 0; i < n; i++)
	{
		cin >> a;
		cout << fixed;
		cout.precision(2);
		cout << "$" << a * 0.8f << "\n";
	}*/

	/*int scoreA, scoreB;

	cin >> scoreA >> scoreB;

	int scoreC = scoreA ^ scoreB;

	cout << scoreC << "\n";*/

	/*int n;

	cin >> n;

	for (int i = 1; i <= n; i++)
	{
		for (int j = 1; j <= i; j++)
		{
			cout << "*";
		}
		cout << "\n";
	}*/

	/*int n, m;

	cin >> n >> m;

	cout << min(n / 2, m) << "\n";*/

}